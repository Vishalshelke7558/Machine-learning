install.packages("LiblineaR") # command for installing packges

library() # to check the installed libraries

search() # to check the library is loaded or not

require("LiblineaR")  # to load the library

search()

? LiblineaR # to know more about library

? ggplot2

require("ggplot2")

? ggplot2

data() # to check avilable datasets

library(help = "datasets") 

? iris # to know more about dataser

search()

str(iris) # to know the structure of the dataset

iris # to see total dataset

data(iris) # to load the dataset
